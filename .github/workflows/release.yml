name: Build and Release Parasyte

on:
  push:
    tags:
      - 'v*' 

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    permissions:
      contents: write 

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11' 

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install nuitka zstandard

      - name: Build Binary with Nuitka
        run: |
          python -m nuitka --standalone --assume-yes-for-downloads --output-dir=dist parasyte.py

      - name: Archive Build (Mac & Linux)
        if: matrix.os != 'windows-latest'
        run: |
          cd dist
          tar -czvf parasyte-${{ matrix.os }}.tar.gz parasyte.dist/

      - name: Archive Build (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          cd dist
          Compress-Archive -Path parasyte.dist -DestinationPath parasyte-${{ matrix.os }}.zip

      - name: Upload to GitHub Releases (Mac & Linux)
        if: matrix.os != 'windows-latest'
        uses: softprops/action-gh-release@v2
        with:
          files: dist/parasyte-${{ matrix.os }}.tar.gz

      - name: Upload to GitHub Releases (Windows)
        if: matrix.os == 'windows-latest'
        uses: softprops/action-gh-release@v2
        with:
          files: dist/parasyte-${{ matrix.os }}.zip
